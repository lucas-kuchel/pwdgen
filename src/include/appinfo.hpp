#pragma once

#include <cstdlib>

namespace appinfo {
    static constexpr std::size_t VERSION_MAJOR = 0;
    static constexpr std::size_t VERSION_MINOR = 1;
    static constexpr std::size_t VERSION_PATCH = 0;
}